<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calendar">
        <activity
            android:name=".RoutineTasks.EditRoutine.ChangeRoutine"
            android:exported="false" />
        <activity
            android:name=".RoutineTasks.RoutineTasks"
            android:exported="false" />
        <activity
            android:name=".Templates.MainTemplate"
            android:exported="false" />
        <activity
            android:name=".BasicTasks.TasksList.TasksList"
            android:exported="false" />
        <activity
            android:name=".BasicTasks.AddTask.AddTask"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>